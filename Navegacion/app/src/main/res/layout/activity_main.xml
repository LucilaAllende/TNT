<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity" >

    <fragment
        android:id="@+id/main_nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:navGraph="@navigation/app_navigation"
        app:defaultNavHost="true"
        />
    <!--
        TODO #4:
            - app:navGraph="@navigation/app_navigation" asocia el NavHostFragment con nuestro
            navigation graph. Este grafo especifica todos los destinos a los que el usuario puede
            navegar con este NavHostFragment.
            - android:name="androidx.navigation.fragment.NavHostFragment" y
             app:defaultNavHost="true" conectan el boton 'atras' del sistema al NavHostFragment
    -->

</LinearLayout>


